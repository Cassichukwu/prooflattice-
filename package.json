{
  "name": "prooflattice-contracts",
  "version": "0.1.0",
  "description": "ProofLattice smart contracts for Mantle Turing Test Hackathon",
  "scripts": {
    "deploy:mantle": "hardhat run scripts/deploy-mantle.js --network mantleSepolia",
    "deploy:local": "hardhat run scripts/deploy-mantle.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "ethers": "^6.13.0"
  }
}
